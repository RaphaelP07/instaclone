<h2>
  Welcome <%= user_signed_in? ? current_user.username : '' %>
</h2>

<div id="posts"></div>
<% Post.all.sort.reverse.each do |post| %>
  <%= render partial: "posts/post", locals: { post: post } %>
<% end %>
